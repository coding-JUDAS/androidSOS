package com.example.androidsos.model.driver;

import com.example.androidsos.model.gameEngine.GameEngine;
import com.example.androidsos.model.gameEngine.Player;
import com.example.androidsos.model.grid.Grid;


public class Tester {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		int size = 3;
		/*for(int i = 0; i < 9; i++) {
			System.out.printf("(" + i/size + "," + i%size + ")");
		}

		Grid grid = Grid.getInstance(3);
		System.out.println(grid);
		Player p1 = new Player(1);
		Player p2 = new Player(2);GameEngine gameEngine = new GameEngine(size,1,2);
		gameEngine.run();*/
	}

}
